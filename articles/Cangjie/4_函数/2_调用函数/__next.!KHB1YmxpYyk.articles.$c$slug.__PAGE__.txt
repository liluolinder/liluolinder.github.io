1:"$Sreact.fragment"
2:I[23597,["/_next/static/chunks/0o--8oop24u_f.js","/_next/static/chunks/3oax6b5zaxt_z.js","/_next/static/chunks/0jubty45h__ir.js","/_next/static/chunks/0g4o6d4sifcpo.js","/_next/static/chunks/1ssz3eabjrzaj.js"],"TocSidebar"]
9:I[59537,["/_next/static/chunks/0o--8oop24u_f.js","/_next/static/chunks/3oax6b5zaxt_z.js","/_next/static/chunks/0jubty45h__ir.js","/_next/static/chunks/0g4o6d4sifcpo.js","/_next/static/chunks/1ssz3eabjrzaj.js"],"ArticleNavSidebar"]
a:I[22016,["/_next/static/chunks/0o--8oop24u_f.js","/_next/static/chunks/3oax6b5zaxt_z.js","/_next/static/chunks/0jubty45h__ir.js","/_next/static/chunks/0g4o6d4sifcpo.js","/_next/static/chunks/1ssz3eabjrzaj.js"],""]
b:I[96438,["/_next/static/chunks/0o--8oop24u_f.js","/_next/static/chunks/3oax6b5zaxt_z.js","/_next/static/chunks/0jubty45h__ir.js","/_next/static/chunks/0g4o6d4sifcpo.js","/_next/static/chunks/1ssz3eabjrzaj.js"],"ArticleContent"]
e:I[97367,["/_next/static/chunks/0o--8oop24u_f.js","/_next/static/chunks/3oax6b5zaxt_z.js","/_next/static/chunks/0jubty45h__ir.js"],"OutletBoundary"]
f:"$Sreact.suspense"
11:I[28093,["/_next/static/chunks/0o--8oop24u_f.js","/_next/static/chunks/3oax6b5zaxt_z.js","/_next/static/chunks/0jubty45h__ir.js","/_next/static/chunks/0g4o6d4sifcpo.js","/_next/static/chunks/1ssz3eabjrzaj.js"],"MobileArticlePanel"]
3:T2ec3,
## 4.2.1 如何调用函数

上节课我们学会了如何定义函数，现在我们来学习怎么"用"它。

函数定义好之后，通过**函数调用**来使用它。基本形式为：

```Cangjie
f(arg1, arg2, ..., argn)
```

其中：
- `f` 是要调用的函数的名字
- `arg1` 到 `argn` 是传递给函数的实参（实际参数）

> [!NOTE]
> 每个实参的类型必须是**对应参数类型的子类型**。新手可以简单理解为：**传入的值类型要和参数定义的类型一致**。
>
> 如果函数定义时参数是 `Int64` 类型，你就不能传一个 `String` 进去。

### 最简单的例子

```Cangjie
func sayHello() {
    println("你好！")
}

main() {
    sayHello()       // ✅ 调用无参函数
}
```

> [!TIP]
> 调用函数时，记得一定要加上 `()`。
> - `sayHello` —— 指的是"这个函数本身"（就像指着机器说"这个机器"）
> - `sayHello()` —— 指的是"调用这个函数"（就像按下了机器的启动按钮）

根据函数定义时参数是**非命名参数**还是**命名参数**，调用时传参的方式也不同：

| 参数类型 | 调用方式 | 示例 |
|---------|---------|------|
| 非命名参数 | 直接传表达式 | `add(x, y)` |
| 命名参数 | 使用 `参数名: 表达式` | `add(x, b: y)` |

---

## 4.2.2 非命名参数调用（按位置传参）

如果函数的参数都是非命名参数，调用时按**位置**依次传参即可：

```Cangjie
func add(a: Int64, b: Int64) {
    return a + b
}

main() {
    let x = 1
    let y = 2
    let r = add(x, y)        // 按位置：第一个值 1 给 a，第二个值 2 给 b
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 3
```

> [!TIP]
> 可以理解为"排队打饭"：第一个来的人（第一个参数值）去第一个窗口（第一个形参），第二个人去第二个窗口。
>
> 所以 `add(x, y)` 中：
> - 第一个值 `x=1` → 第一个参数 `a`
> - 第二个值 `y=2` → 第二个参数 `b`
>
> **顺序很重要！** 如果定义时是 `add(a, b)`，但你传了 `add(y, x)`，那就变成 `a=y`、`b=x` 了。

---

## 4.2.3 命名参数调用（按名称传参）

如果函数的参数是命名参数（定义时参数名后带 `!`），调用时需要指定参数名：

```Cangjie
func add(a: Int64, b!: Int64) {
    return a + b
}

main() {
    let x = 1
    let y = 2
    let r = add(x, b: y)     // a 是非命名参数（按位置），b 是命名参数（按名称）
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 3
```

### 命名参数的顺序可以调换

对于多个命名参数，调用时的传参顺序**可以和定义时的参数顺序不同**：

```Cangjie
func add(a!: Int64, b!: Int64) {
    return a + b
}

main() {
    let x = 1
    let y = 2
    let r = add(b: y, a: x)   // ✅ 命名参数可以调换顺序
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 3
```

> [!IMPORTANT]
> 非命名参数和命名参数混用时：
> 1. 非命名参数**必须在前**，且按位置传参
> 2. 命名参数**必须在后**，按名称传参
>
> 就像排队买东西：非命名参数是"按顺序排队"，命名参数是"叫号入场"——号在前的人（非命名）先处理，叫号的人（命名）后处理。

---

## 4.2.4 默认参数调用

对于拥有默认值的命名参数，调用时如果没有传实参，那么该参数将使用定义时的默认值：

```Cangjie
func add(a: Int64, b!: Int64 = 2) {  // b 的默认值是 2
    return a + b
}

main() {
    let x = 1
    let r = add(x)             // b 没传，使用默认值 2 → 1 + 2 = 3
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 3
```

调用时也可以为有默认值的参数传入**新值**，此时默认值被覆盖：

```Cangjie
func add(a: Int64, b!: Int64 = 2) {
    return a + b
}

main() {
    let x = 1
    let r = add(x, b: 20)      // b 传了新值 20，覆盖默认值 2 → 1 + 20 = 21
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 21
```

> [!TIP]
> 默认值就像"自动售货机的默认选项"——你什么都不按，它就给你默认的商品；你按了别的按钮，就给其他的。
>
> 默认值特别适合：
> - 大部分情况下参数值都一样，但偶尔需要改（如：`timeout: Int64 = 30`）
> - 函数有很多参数，但大多数参数有合理的默认值

---

## 4.2.5 流操作符（让代码像流水线一样清晰）

仓颉提供了两个流操作符，用于简化数据的链式处理和函数组合。这是仓颉的一个特色功能。

### Pipeline 表达式 `|>`（数据流水线）

> [!TIP]
> `|>` 的英文名叫 pipeline，中文叫"流水线"——就像工厂里的传送带，原材料从一头进去，经过一道道工序，从另一头出来成品。

当需要对输入数据做一系列处理时，可以用 pipeline 表达式来让代码更直观。

**语法形式**：

```Cangjie
e1 |> e2
```

这等价于：

```Cangjie  
let v = e1
e2(v)
```

也就是：**把 `e1` 的结果作为参数，传给 `e2` 函数**。

**举个实际例子**——先给数组每个元素加 1，再求和：

```Cangjie
// 数组每个元素加 1
func inc(x: Array<Int64>): Array<Int64> {
    let s = x.size
    var i = 0
    for (e in x where i < s) {
        x[i] = e + 1
        i++
    }
    x
}

// 数组元素求和
func sum(y: Array<Int64>): Int64 {
    var s = 0
    for (j in y) {
        s += j
    }
    s
}

main() {
    let arr: Array<Int64> = [1, 3, 5]
    
    // 用 pipeline 的方式（推荐）：
    let res = arr |> inc |> sum      // 先 inc 再 sum —— 从左到右读，像流水线
    println(res)                     // 输出：12
    
    // 不用 pipeline 的方式（嵌套调用，从里往外读，容易晕）：
    // let res = sum(inc(arr))       // 先执行 inc(arr)，再把结果传给 sum
}
```

执行结果为：

```Bash
12
```

> [!NOTE]
> `arr |> inc |> sum` 看起来就像"数据在流动"：数组先流进 `inc` 处理，结果再流进 `sum` 求和。而嵌套写法 `sum(inc(arr))` 需要从最内层开始往外面读，不够直观。

### Composition 表达式 `~>`（函数组合）

composition 表达式表示**把两个单参函数组合成一个新函数**。

**语法形式**：

```Cangjie
f ~> g
```

这等价于：

```Cangjie
{ x => g(f(x)) }
```

也就是：先对 `x` 执行 `f`，再把结果传给 `g`。

> [!TIP]
> 可以把 `~>` 想象成"乐高积木"——把两个小积木（函数）拼在一起，变成一块大的积木（新函数）。

**示例**：

```Cangjie
func f(x: Int64): Float64 {
    Float64(x)          // Int64 → Float64
}

func g(x: Float64): Float64 {
    x + 1.0             // 加 1
}

var fg = f ~> g        // 组合：先把 Int64 转 Float64，再加 1
// fg 等价于 { x: Int64 => g(f(x)) }
```

> [!NOTE]
> 表达式 `f ~> g` 中，会先对 `f` 求值，再对 `g` 求值，最后才组合。组合后的新函数类型是 `(Int64) -> Float64`。

### 流操作符与命名参数的配合

流操作符**不能**与无默认值的命名形参函数直接一起使用——因为无默认值的命名形参函数必须给出命名实参才能调用：

```Cangjie
func f(a!: Int64): Unit {}

var a = 1 |> f // Error
```

通过 lambda 表达式可以解决（lambda 会在后续文章介绍）：

```Cangjie
func f(a!: Int64): Unit {}

var x = 1 |> { x: Int64 => f(a: x) } // ✅ 用 lambda 传命名参数
```

同样，当命名参数有默认值时，直接与流操作符一起使用也是错误的：

```Cangjie
func f(a!: Int64 = 2): Unit {}

var a = 1 |> f // Error
```

但是如果命名形参都有默认值，且函数只需要传入非命名形参，则可以与流操作符一起使用：

```Cangjie
func f(a: Int64, b!: Int64 = 2): Unit {}

var a = 1 |> f // ✅ OK，a 是非命名参数
```

> [!TIP]
> **简单记忆**：`|>` 只能传递一个值。如果函数除了这个值还需要其他命名参数，就得用 lambda 包装一下。

---

## 4.2.6 变长参数（一次传任意多个值）

变长参数是一种特殊的函数调用语法糖。当形参的**最后一个非命名参数**是 `Array` 类型时，实参中对应位置可以直接传入参数序列来代替手动写 `[]`：

```Cangjie
func sum(arr: Array<Int64>) {
    var total = 0
    for (x in arr) {
        total += x
    }
    return total
}

main() {
    println(sum())          // 0 个参数 → arr = []
    println(sum(1, 2, 3))   // 3 个参数 → arr = [1, 2, 3]
}
```

程序输出：

```Bash
0
6
```

> [!TIP]
> 变长参数就像"超市的自助称重"——你可以放一个苹果（1 个参数），也可以放一堆苹果（多个参数），称重台都能处理。
>
> 如果没有变长参数，你就得自己写 `sum([])` 和 `sum([1, 2, 3])`——多写一对方括号。变长参数帮你省掉了这步。

> [!IMPORTANT]
> - 只有**最后一个非命名参数**可以作为变长参数
> - 命名参数**不能**使用变长参数语法

```Cangjie
func length(arr!: Array<Int64>) {
    return arr.size
}

main() {
    println(length())        // Error: 命名参数不能变长
    println(length(1, 2, 3)) // Error: 命名参数不能变长
}
```

### 变长参数的使用范围

变长参数可以用在很多地方：

- ✅ 全局函数
- ✅ 静态成员函数
- ✅ 实例成员函数
- ✅ 局部函数
- ✅ 构造函数
- ✅ 函数变量和 lambda
- ❌ composition（`~>`）和 pipeline（`|>`）不支持

> [!NOTE]
> class 相关内容将在后续讲解，此处先留个印象。

```Cangjie
class Counter {
    var total = 0
    init(data: Array<Int64>) {
        total = data.size
    }
    operator func ()(data: Array<Int64>) {
        total += data.size
    }
}

main() {
    let counter = Counter(1, 2)    // 两个参数 → data = [1, 2]，total = 2
    println(counter.total)         // 输出：2
    counter(3, 4, 5)               // 三个参数 → data = [3, 4, 5]，total = 2 + 3 = 5
    println(counter.total)         // 输出：5
}
```

### 变长参数与函数重载

当同时有"普通版本"和"变长版本"的函数时，编译器会**优先匹配普通版本**：

```Cangjie
func f<T>(x: T) where T <: ToString {
    println("item: ${x}")
}

func f(arr: Array<Int64>) {
    println("array: ${arr}")
}

main() {
    f()       // 无参 → 只能匹配变长版本 → array: []
    f(1)      // 1 个参数 → 优先匹配普通版本 f<T>(x: T) → item: 1
    f(1, 2)   // 2 个参数 → 普通版本不匹配 → 变长版本 → array: [1, 2]
}
```

```Bash
array: []
item: 1
array: [1, 2]
```

> [!WARNING]
> 如果两个变长版本都能匹配，编译器无法决定用哪个，会报错。解决办法是给函数改名或者调整参数设计：
> ```Cangjie
> func f(arr: Array<Int64>) { arr.size }
> func f(first: Int64, arr: Array<Int64>) { first + arr.size }
> main() {
>     println(f(1, 2, 3)) // Error：两个变长函数都能匹配
> }
> ```

---

## 4.2.7 总结

| 知识点 | 说明 |
|--------|------|
| 基本调用 | `f(arg1, arg2, ...)`，按位置传参 |
| 非命名参数 | 按位置传参，顺序必须与定义一致——像"排队打饭" |
| 命名参数 | 按名称传参，使用 `p: e` 语法，顺序可调换——像"快递标签" |
| 默认参数 | 调用时可不传，使用定义时的默认值——像"自动售货机默认选项" |
| Pipeline `\|>` | `e1 \|> e2` 等价于 `e2(e1)`，数据像流水线一样从左流到右 |
| Composition `~>` | `f ~> g` 等价于 `{ x => g(f(x)) }`，像拼乐高一样组合函数 |
| 变长参数 | 最后一个非命名 Array 参数可接收 0 或多个实参——像"自助称重" |
| 重载决议 | 优先匹配非变长版本，不行才尝试变长版本 |
0:{"rsc":["$","$1","c",{"children":[["$","div",null,{"className":"min-h-screen pb-16","children":[["$","$L2",null,{"content":"$3"}],"$L4","$L5"]}],["$L6","$L7"],"$L8"]}],"isPartial":false,"staleTime":300,"varyParams":null,"buildId":"vKJemjeJRPs_ky0q42LE4"}
4:["$","aside",null,{"className":"fixed left-6 top-1/2 -translate-y-1/2 w-[240px] hidden xl:block z-10","children":["$","div",null,{"className":"liquid-glass rounded-2xl p-5 space-y-3 max-h-[70vh] overflow-y-auto","children":[["$","h3",null,{"className":"text-xs font-bold text-gray-500 dark:text-gray-400 uppercase tracking-wider","children":"合集"}],["$","div",null,{"className":"w-full h-px bg-blue-100/50 dark:bg-blue-900/30"}],["$","$L9",null,{"rootPath":"Cangjie","rootName":"Cangjie","articles":[{"slug":"Cangjie/1_初识仓颉/0_前言","title":"初识仓颉","description":"从零开始，走进仓颉的世界","date":"2026-06-24","tags":["Cangjie","入门教程"],"collection":"Cangjie/1_初识仓颉","sort":0,"published":true,"mtime":1782986047850.0527},{"slug":"Cangjie/2_基本数据类型/0_前言","title":"前言","description":"仓颉的基本数据类型详解","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/2_基本数据类型","sort":0,"published":true,"mtime":1782986047850.126},{"slug":"Cangjie/3_逻辑控制/0_前言","title":"逻辑控制","description":"仓颉的逻辑控制——分支与循环","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/3_逻辑控制","sort":0,"published":true,"mtime":1782986047850.484},{"slug":"Cangjie/4_函数/0_前言","title":"前言","description":"仓颉的函数——从基础到进阶","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/4_函数","sort":0,"published":true,"mtime":1782986047851.4631},{"slug":"Cangjie/1_初识仓颉/1_第一个仓颉程序","title":"第一个仓颉程序","description":"步入Cangjie的世界，从零运行起你的第一个程序","date":"2026-06-24","tags":["Cangjie","入门教程"],"collection":"Cangjie/1_初识仓颉","sort":1,"published":true,"mtime":1782986047850.126},{"slug":"Cangjie/2_基本数据类型/1_整数类型","title":"整数类型","description":"仓颉的基本数据类型","date":"2026-06-24","tags":["Cangjie","入门教程"],"collection":"Cangjie/2_基本数据类型","sort":1,"published":true,"mtime":1782986047850.484},{"slug":"Cangjie/3_逻辑控制/1_if分支结构","title":"if分支结构","description":"仓颉的逻辑控制——if分支结构","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/3_逻辑控制","sort":1,"published":true,"mtime":1782986047851.328},{"slug":"Cangjie/4_函数/1_定义函数","title":"定义函数","description":"学会编写可复用的代码块","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/4_函数","sort":1,"published":true,"mtime":1782986047851.8804},{"slug":"Cangjie/1_初识仓颉/2_仓颉的注释","title":"仓颉的注释","description":"为程序解释含义","date":"2026-06-24","tags":["Cangjie","入门教程"],"collection":"Cangjie/1_初识仓颉","sort":2,"published":true,"mtime":1782986047850.126},{"slug":"Cangjie/2_基本数据类型/2_浮点类型","title":"浮点类型","description":"仓颉的基本数据类型","date":"2026-06-24","tags":["Cangjie","入门教程"],"collection":"Cangjie/2_基本数据类型","sort":2,"published":true,"mtime":1782986047850.484},{"slug":"Cangjie/3_逻辑控制/2_match分支结构","title":"match分支结构","description":"仓颉的逻辑控制——match分支结构","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/3_逻辑控制","sort":2,"published":true,"mtime":1782986047851.328},{"slug":"Cangjie/4_函数/2_调用函数","title":"调用函数","description":"学会灵活调用仓颉函数","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/4_函数","sort":2,"published":true,"mtime":1782986047851.8804},{"slug":"Cangjie/1_初识仓颉/3_仓颉的标识符","title":"仓颉的标识符","description":"为程序元素命名","date":"2026-06-24","tags":["Cangjie","入门教程"],"collection":"Cangjie/1_初识仓颉","sort":3,"published":true,"mtime":1782986047850.126},{"slug":"Cangjie/2_基本数据类型/3_字符型","title":"字符型","description":"仓颉的基本数据类型","date":"2026-06-24","tags":["Cangjie","入门教程"],"collection":"Cangjie/2_基本数据类型","sort":3,"published":true,"mtime":1782986047850.484},{"slug":"Cangjie/3_逻辑控制/3_while循环结构","title":"while循环结构","description":"仓颉的逻辑控制——while循环结构","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/3_逻辑控制","sort":3,"published":true,"mtime":1782986047851.328},{"slug":"Cangjie/4_函数/3_函数类型","title":"函数类型","description":"仓颉中函数也是一等公民","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/4_函数","sort":3,"published":true,"mtime":1782986047851.8804},{"slug":"Cangjie/1_初识仓颉/4_仓颉的变量","title":"仓颉的变量","description":"更方便的管理数据","date":"2026-06-24","tags":["Cangjie","入门教程"],"collection":"Cangjie/1_初识仓颉","sort":4,"published":true,"mtime":1782986047850.126},{"slug":"Cangjie/2_基本数据类型/4_字符串型","title":"字符串型","description":"仓颉的基本数据类型","date":"2026-06-24","tags":["Cangjie","入门教程"],"collection":"Cangjie/2_基本数据类型","sort":4,"published":true,"mtime":1782986047850.484},{"slug":"Cangjie/3_逻辑控制/4_do-while循环结构","title":"do-while循环结构","description":"仓颉的逻辑控制——do-while循环结构","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/3_逻辑控制","sort":4,"published":true,"mtime":1782986047851.328},{"slug":"Cangjie/4_函数/4_lambda表达式","title":"Lambda表达式","description":"快速创建匿名函数","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/4_函数","sort":4,"published":true,"mtime":1782986047851.8804},{"slug":"Cangjie/2_基本数据类型/5_布尔类型","title":"布尔类型","description":"仓颉的基本数据类型","date":"2026-06-24","tags":["Cangjie","入门教程"],"collection":"Cangjie/2_基本数据类型","sort":5,"published":true,"mtime":1782986047850.484},{"slug":"Cangjie/3_逻辑控制/5_for-in循环结构","title":"for-in循环结构","description":"仓颉的逻辑控制——for-in循环结构","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/3_逻辑控制","sort":5,"published":true,"mtime":1782986047851.328},{"slug":"Cangjie/4_函数/5_嵌套函数","title":"嵌套函数","description":"函数内部定义函数","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/4_函数","sort":5,"published":true,"mtime":1782986047851.8804},{"slug":"Cangjie/2_基本数据类型/6_元组类型","title":"元组类型","description":"仓颉的基本数据类型","date":"2026-06-24","tags":["Cangjie","入门教程"],"collection":"Cangjie/2_基本数据类型","sort":6,"published":true,"mtime":1782986047850.484},{"slug":"Cangjie/3_逻辑控制/6_循环控制","title":"循环控制","description":"仓颉的逻辑控制——break 与 continue","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/3_逻辑控制","sort":6,"published":true,"mtime":1782986047851.4631},{"slug":"Cangjie/4_函数/6_闭包","title":"闭包","description":"捕获变量的函数与lambda","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/4_函数","sort":6,"published":true,"mtime":1782986047851.8804},{"slug":"Cangjie/2_基本数据类型/7_数组类型","title":"数组类型","description":"仓颉的基本数据类型","date":"2026-06-25","tags":["Cangjie","入门教程"],"collection":"Cangjie/2_基本数据类型","sort":7,"published":true,"mtime":1782986047850.484},{"slug":"Cangjie/4_函数/7_函数重载","title":"函数重载","description":"同名函数的不同形态","date":"2026-07-02","tags":["Cangjie","入门教程"],"collection":"Cangjie/4_函数","sort":7,"published":true,"mtime":1782986047851.8804},{"slug":"Cangjie/2_基本数据类型/8_区间类型","title":"区间类型","description":"仓颉的基本数据类型——区间类型","date":"2026-06-25","tags":["Cangjie","入门教程"],"collection":"Cangjie/2_基本数据类型","sort":8,"published":true,"mtime":1782986047850.484},{"slug":"Cangjie/2_基本数据类型/9_Unit类型","title":"Unit类型","description":"仓颉的基本数据类型——Unit类型","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/2_基本数据类型","sort":9,"published":true,"mtime":1782986047850.484},{"slug":"Cangjie/2_基本数据类型/10_nothing类型","title":"Nothing类型","description":"仓颉的基本数据类型——Nothing类型","date":"2026-07-01","tags":["Cangjie","入门教程"],"collection":"Cangjie/2_基本数据类型","sort":10,"published":true,"mtime":1782986047850.484},{"slug":"Cangjie/2_基本数据类型/11_运算符","title":"运算符","description":"仓颉的基本运算操作符","date":"2026-06-24","tags":["Cangjie","入门教程"],"collection":"Cangjie/2_基本数据类型","sort":11,"published":true,"mtime":1782986047850.484},{"slug":"Cangjie/2_基本数据类型/12_类型转换","title":"类型转换","description":"仓颉的基本数据类型——数值类型转换","date":"2026-06-24","tags":["Cangjie","入门教程"],"collection":"Cangjie/2_基本数据类型","sort":12,"published":true,"mtime":1782986047850.484}],"currentSlug":"Cangjie/4_函数/2_调用函数"}]]}]}]
c:T2ec3,
## 4.2.1 如何调用函数

上节课我们学会了如何定义函数，现在我们来学习怎么"用"它。

函数定义好之后，通过**函数调用**来使用它。基本形式为：

```Cangjie
f(arg1, arg2, ..., argn)
```

其中：
- `f` 是要调用的函数的名字
- `arg1` 到 `argn` 是传递给函数的实参（实际参数）

> [!NOTE]
> 每个实参的类型必须是**对应参数类型的子类型**。新手可以简单理解为：**传入的值类型要和参数定义的类型一致**。
>
> 如果函数定义时参数是 `Int64` 类型，你就不能传一个 `String` 进去。

### 最简单的例子

```Cangjie
func sayHello() {
    println("你好！")
}

main() {
    sayHello()       // ✅ 调用无参函数
}
```

> [!TIP]
> 调用函数时，记得一定要加上 `()`。
> - `sayHello` —— 指的是"这个函数本身"（就像指着机器说"这个机器"）
> - `sayHello()` —— 指的是"调用这个函数"（就像按下了机器的启动按钮）

根据函数定义时参数是**非命名参数**还是**命名参数**，调用时传参的方式也不同：

| 参数类型 | 调用方式 | 示例 |
|---------|---------|------|
| 非命名参数 | 直接传表达式 | `add(x, y)` |
| 命名参数 | 使用 `参数名: 表达式` | `add(x, b: y)` |

---

## 4.2.2 非命名参数调用（按位置传参）

如果函数的参数都是非命名参数，调用时按**位置**依次传参即可：

```Cangjie
func add(a: Int64, b: Int64) {
    return a + b
}

main() {
    let x = 1
    let y = 2
    let r = add(x, y)        // 按位置：第一个值 1 给 a，第二个值 2 给 b
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 3
```

> [!TIP]
> 可以理解为"排队打饭"：第一个来的人（第一个参数值）去第一个窗口（第一个形参），第二个人去第二个窗口。
>
> 所以 `add(x, y)` 中：
> - 第一个值 `x=1` → 第一个参数 `a`
> - 第二个值 `y=2` → 第二个参数 `b`
>
> **顺序很重要！** 如果定义时是 `add(a, b)`，但你传了 `add(y, x)`，那就变成 `a=y`、`b=x` 了。

---

## 4.2.3 命名参数调用（按名称传参）

如果函数的参数是命名参数（定义时参数名后带 `!`），调用时需要指定参数名：

```Cangjie
func add(a: Int64, b!: Int64) {
    return a + b
}

main() {
    let x = 1
    let y = 2
    let r = add(x, b: y)     // a 是非命名参数（按位置），b 是命名参数（按名称）
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 3
```

### 命名参数的顺序可以调换

对于多个命名参数，调用时的传参顺序**可以和定义时的参数顺序不同**：

```Cangjie
func add(a!: Int64, b!: Int64) {
    return a + b
}

main() {
    let x = 1
    let y = 2
    let r = add(b: y, a: x)   // ✅ 命名参数可以调换顺序
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 3
```

> [!IMPORTANT]
> 非命名参数和命名参数混用时：
> 1. 非命名参数**必须在前**，且按位置传参
> 2. 命名参数**必须在后**，按名称传参
>
> 就像排队买东西：非命名参数是"按顺序排队"，命名参数是"叫号入场"——号在前的人（非命名）先处理，叫号的人（命名）后处理。

---

## 4.2.4 默认参数调用

对于拥有默认值的命名参数，调用时如果没有传实参，那么该参数将使用定义时的默认值：

```Cangjie
func add(a: Int64, b!: Int64 = 2) {  // b 的默认值是 2
    return a + b
}

main() {
    let x = 1
    let r = add(x)             // b 没传，使用默认值 2 → 1 + 2 = 3
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 3
```

调用时也可以为有默认值的参数传入**新值**，此时默认值被覆盖：

```Cangjie
func add(a: Int64, b!: Int64 = 2) {
    return a + b
}

main() {
    let x = 1
    let r = add(x, b: 20)      // b 传了新值 20，覆盖默认值 2 → 1 + 20 = 21
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 21
```

> [!TIP]
> 默认值就像"自动售货机的默认选项"——你什么都不按，它就给你默认的商品；你按了别的按钮，就给其他的。
>
> 默认值特别适合：
> - 大部分情况下参数值都一样，但偶尔需要改（如：`timeout: Int64 = 30`）
> - 函数有很多参数，但大多数参数有合理的默认值

---

## 4.2.5 流操作符（让代码像流水线一样清晰）

仓颉提供了两个流操作符，用于简化数据的链式处理和函数组合。这是仓颉的一个特色功能。

### Pipeline 表达式 `|>`（数据流水线）

> [!TIP]
> `|>` 的英文名叫 pipeline，中文叫"流水线"——就像工厂里的传送带，原材料从一头进去，经过一道道工序，从另一头出来成品。

当需要对输入数据做一系列处理时，可以用 pipeline 表达式来让代码更直观。

**语法形式**：

```Cangjie
e1 |> e2
```

这等价于：

```Cangjie  
let v = e1
e2(v)
```

也就是：**把 `e1` 的结果作为参数，传给 `e2` 函数**。

**举个实际例子**——先给数组每个元素加 1，再求和：

```Cangjie
// 数组每个元素加 1
func inc(x: Array<Int64>): Array<Int64> {
    let s = x.size
    var i = 0
    for (e in x where i < s) {
        x[i] = e + 1
        i++
    }
    x
}

// 数组元素求和
func sum(y: Array<Int64>): Int64 {
    var s = 0
    for (j in y) {
        s += j
    }
    s
}

main() {
    let arr: Array<Int64> = [1, 3, 5]
    
    // 用 pipeline 的方式（推荐）：
    let res = arr |> inc |> sum      // 先 inc 再 sum —— 从左到右读，像流水线
    println(res)                     // 输出：12
    
    // 不用 pipeline 的方式（嵌套调用，从里往外读，容易晕）：
    // let res = sum(inc(arr))       // 先执行 inc(arr)，再把结果传给 sum
}
```

执行结果为：

```Bash
12
```

> [!NOTE]
> `arr |> inc |> sum` 看起来就像"数据在流动"：数组先流进 `inc` 处理，结果再流进 `sum` 求和。而嵌套写法 `sum(inc(arr))` 需要从最内层开始往外面读，不够直观。

### Composition 表达式 `~>`（函数组合）

composition 表达式表示**把两个单参函数组合成一个新函数**。

**语法形式**：

```Cangjie
f ~> g
```

这等价于：

```Cangjie
{ x => g(f(x)) }
```

也就是：先对 `x` 执行 `f`，再把结果传给 `g`。

> [!TIP]
> 可以把 `~>` 想象成"乐高积木"——把两个小积木（函数）拼在一起，变成一块大的积木（新函数）。

**示例**：

```Cangjie
func f(x: Int64): Float64 {
    Float64(x)          // Int64 → Float64
}

func g(x: Float64): Float64 {
    x + 1.0             // 加 1
}

var fg = f ~> g        // 组合：先把 Int64 转 Float64，再加 1
// fg 等价于 { x: Int64 => g(f(x)) }
```

> [!NOTE]
> 表达式 `f ~> g` 中，会先对 `f` 求值，再对 `g` 求值，最后才组合。组合后的新函数类型是 `(Int64) -> Float64`。

### 流操作符与命名参数的配合

流操作符**不能**与无默认值的命名形参函数直接一起使用——因为无默认值的命名形参函数必须给出命名实参才能调用：

```Cangjie
func f(a!: Int64): Unit {}

var a = 1 |> f // Error
```

通过 lambda 表达式可以解决（lambda 会在后续文章介绍）：

```Cangjie
func f(a!: Int64): Unit {}

var x = 1 |> { x: Int64 => f(a: x) } // ✅ 用 lambda 传命名参数
```

同样，当命名参数有默认值时，直接与流操作符一起使用也是错误的：

```Cangjie
func f(a!: Int64 = 2): Unit {}

var a = 1 |> f // Error
```

但是如果命名形参都有默认值，且函数只需要传入非命名形参，则可以与流操作符一起使用：

```Cangjie
func f(a: Int64, b!: Int64 = 2): Unit {}

var a = 1 |> f // ✅ OK，a 是非命名参数
```

> [!TIP]
> **简单记忆**：`|>` 只能传递一个值。如果函数除了这个值还需要其他命名参数，就得用 lambda 包装一下。

---

## 4.2.6 变长参数（一次传任意多个值）

变长参数是一种特殊的函数调用语法糖。当形参的**最后一个非命名参数**是 `Array` 类型时，实参中对应位置可以直接传入参数序列来代替手动写 `[]`：

```Cangjie
func sum(arr: Array<Int64>) {
    var total = 0
    for (x in arr) {
        total += x
    }
    return total
}

main() {
    println(sum())          // 0 个参数 → arr = []
    println(sum(1, 2, 3))   // 3 个参数 → arr = [1, 2, 3]
}
```

程序输出：

```Bash
0
6
```

> [!TIP]
> 变长参数就像"超市的自助称重"——你可以放一个苹果（1 个参数），也可以放一堆苹果（多个参数），称重台都能处理。
>
> 如果没有变长参数，你就得自己写 `sum([])` 和 `sum([1, 2, 3])`——多写一对方括号。变长参数帮你省掉了这步。

> [!IMPORTANT]
> - 只有**最后一个非命名参数**可以作为变长参数
> - 命名参数**不能**使用变长参数语法

```Cangjie
func length(arr!: Array<Int64>) {
    return arr.size
}

main() {
    println(length())        // Error: 命名参数不能变长
    println(length(1, 2, 3)) // Error: 命名参数不能变长
}
```

### 变长参数的使用范围

变长参数可以用在很多地方：

- ✅ 全局函数
- ✅ 静态成员函数
- ✅ 实例成员函数
- ✅ 局部函数
- ✅ 构造函数
- ✅ 函数变量和 lambda
- ❌ composition（`~>`）和 pipeline（`|>`）不支持

> [!NOTE]
> class 相关内容将在后续讲解，此处先留个印象。

```Cangjie
class Counter {
    var total = 0
    init(data: Array<Int64>) {
        total = data.size
    }
    operator func ()(data: Array<Int64>) {
        total += data.size
    }
}

main() {
    let counter = Counter(1, 2)    // 两个参数 → data = [1, 2]，total = 2
    println(counter.total)         // 输出：2
    counter(3, 4, 5)               // 三个参数 → data = [3, 4, 5]，total = 2 + 3 = 5
    println(counter.total)         // 输出：5
}
```

### 变长参数与函数重载

当同时有"普通版本"和"变长版本"的函数时，编译器会**优先匹配普通版本**：

```Cangjie
func f<T>(x: T) where T <: ToString {
    println("item: ${x}")
}

func f(arr: Array<Int64>) {
    println("array: ${arr}")
}

main() {
    f()       // 无参 → 只能匹配变长版本 → array: []
    f(1)      // 1 个参数 → 优先匹配普通版本 f<T>(x: T) → item: 1
    f(1, 2)   // 2 个参数 → 普通版本不匹配 → 变长版本 → array: [1, 2]
}
```

```Bash
array: []
item: 1
array: [1, 2]
```

> [!WARNING]
> 如果两个变长版本都能匹配，编译器无法决定用哪个，会报错。解决办法是给函数改名或者调整参数设计：
> ```Cangjie
> func f(arr: Array<Int64>) { arr.size }
> func f(first: Int64, arr: Array<Int64>) { first + arr.size }
> main() {
>     println(f(1, 2, 3)) // Error：两个变长函数都能匹配
> }
> ```

---

## 4.2.7 总结

| 知识点 | 说明 |
|--------|------|
| 基本调用 | `f(arg1, arg2, ...)`，按位置传参 |
| 非命名参数 | 按位置传参，顺序必须与定义一致——像"排队打饭" |
| 命名参数 | 按名称传参，使用 `p: e` 语法，顺序可调换——像"快递标签" |
| 默认参数 | 调用时可不传，使用定义时的默认值——像"自动售货机默认选项" |
| Pipeline `\|>` | `e1 \|> e2` 等价于 `e2(e1)`，数据像流水线一样从左流到右 |
| Composition `~>` | `f ~> g` 等价于 `{ x => g(f(x)) }`，像拼乐高一样组合函数 |
| 变长参数 | 最后一个非命名 Array 参数可接收 0 或多个实参——像"自助称重" |
| 重载决议 | 优先匹配非变长版本，不行才尝试变长版本 |
5:["$","div",null,{"className":"xl:pl-[320px] xl:pr-[260px] pt-28 pb-16 px-4","children":["$","div",null,{"className":"max-w-4xl mx-auto","children":[["$","$La",null,{"href":"/articles","className":"inline-flex items-center gap-1 text-sm text-gray-600 dark:text-gray-400 hover:text-blue-500 dark:hover:text-blue-400 mb-6 transition-colors","children":"← 返回文章列表"}],["$","div",null,{"className":"mb-8","children":[["$","div",null,{"className":"flex items-center gap-3 mb-3","children":[["$","span",null,{"className":"text-sm text-gray-600 dark:text-gray-400","children":"2026-07-01"}],["$","div",null,{"className":"flex gap-1.5","children":[["$","span","Cangjie",{"className":"text-xs px-2.5 py-0.5 rounded-full bg-blue-50 dark:bg-blue-900/30 text-blue-500 dark:text-blue-400","children":["#","Cangjie"]}],["$","span","入门教程",{"className":"text-xs px-2.5 py-0.5 rounded-full bg-blue-50 dark:bg-blue-900/30 text-blue-500 dark:text-blue-400","children":["#","入门教程"]}]]}]]}],["$","h1",null,{"className":"text-3xl md:text-4xl font-bold text-gray-800 dark:text-gray-200 leading-tight","children":"调用函数"}],["$","p",null,{"className":"mt-3 text-gray-700 dark:text-gray-300 text-lg","children":"学会灵活调用仓颉函数"}]]}],["$","$Lb",null,{"content":"$c"}],"$Ld"]}]}]
6:["$","script","script-0",{"src":"/_next/static/chunks/0g4o6d4sifcpo.js","async":true}]
7:["$","script","script-1",{"src":"/_next/static/chunks/1ssz3eabjrzaj.js","async":true}]
8:["$","$Le",null,{"children":["$","$f",null,{"name":"Next.MetadataOutlet","children":"$@10"}]}]
12:T2ec3,
## 4.2.1 如何调用函数

上节课我们学会了如何定义函数，现在我们来学习怎么"用"它。

函数定义好之后，通过**函数调用**来使用它。基本形式为：

```Cangjie
f(arg1, arg2, ..., argn)
```

其中：
- `f` 是要调用的函数的名字
- `arg1` 到 `argn` 是传递给函数的实参（实际参数）

> [!NOTE]
> 每个实参的类型必须是**对应参数类型的子类型**。新手可以简单理解为：**传入的值类型要和参数定义的类型一致**。
>
> 如果函数定义时参数是 `Int64` 类型，你就不能传一个 `String` 进去。

### 最简单的例子

```Cangjie
func sayHello() {
    println("你好！")
}

main() {
    sayHello()       // ✅ 调用无参函数
}
```

> [!TIP]
> 调用函数时，记得一定要加上 `()`。
> - `sayHello` —— 指的是"这个函数本身"（就像指着机器说"这个机器"）
> - `sayHello()` —— 指的是"调用这个函数"（就像按下了机器的启动按钮）

根据函数定义时参数是**非命名参数**还是**命名参数**，调用时传参的方式也不同：

| 参数类型 | 调用方式 | 示例 |
|---------|---------|------|
| 非命名参数 | 直接传表达式 | `add(x, y)` |
| 命名参数 | 使用 `参数名: 表达式` | `add(x, b: y)` |

---

## 4.2.2 非命名参数调用（按位置传参）

如果函数的参数都是非命名参数，调用时按**位置**依次传参即可：

```Cangjie
func add(a: Int64, b: Int64) {
    return a + b
}

main() {
    let x = 1
    let y = 2
    let r = add(x, y)        // 按位置：第一个值 1 给 a，第二个值 2 给 b
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 3
```

> [!TIP]
> 可以理解为"排队打饭"：第一个来的人（第一个参数值）去第一个窗口（第一个形参），第二个人去第二个窗口。
>
> 所以 `add(x, y)` 中：
> - 第一个值 `x=1` → 第一个参数 `a`
> - 第二个值 `y=2` → 第二个参数 `b`
>
> **顺序很重要！** 如果定义时是 `add(a, b)`，但你传了 `add(y, x)`，那就变成 `a=y`、`b=x` 了。

---

## 4.2.3 命名参数调用（按名称传参）

如果函数的参数是命名参数（定义时参数名后带 `!`），调用时需要指定参数名：

```Cangjie
func add(a: Int64, b!: Int64) {
    return a + b
}

main() {
    let x = 1
    let y = 2
    let r = add(x, b: y)     // a 是非命名参数（按位置），b 是命名参数（按名称）
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 3
```

### 命名参数的顺序可以调换

对于多个命名参数，调用时的传参顺序**可以和定义时的参数顺序不同**：

```Cangjie
func add(a!: Int64, b!: Int64) {
    return a + b
}

main() {
    let x = 1
    let y = 2
    let r = add(b: y, a: x)   // ✅ 命名参数可以调换顺序
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 3
```

> [!IMPORTANT]
> 非命名参数和命名参数混用时：
> 1. 非命名参数**必须在前**，且按位置传参
> 2. 命名参数**必须在后**，按名称传参
>
> 就像排队买东西：非命名参数是"按顺序排队"，命名参数是"叫号入场"——号在前的人（非命名）先处理，叫号的人（命名）后处理。

---

## 4.2.4 默认参数调用

对于拥有默认值的命名参数，调用时如果没有传实参，那么该参数将使用定义时的默认值：

```Cangjie
func add(a: Int64, b!: Int64 = 2) {  // b 的默认值是 2
    return a + b
}

main() {
    let x = 1
    let r = add(x)             // b 没传，使用默认值 2 → 1 + 2 = 3
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 3
```

调用时也可以为有默认值的参数传入**新值**，此时默认值被覆盖：

```Cangjie
func add(a: Int64, b!: Int64 = 2) {
    return a + b
}

main() {
    let x = 1
    let r = add(x, b: 20)      // b 传了新值 20，覆盖默认值 2 → 1 + 20 = 21
    println("The sum of x and y is ${r}")
}
```

执行结果为：

```Bash
The sum of x and y is 21
```

> [!TIP]
> 默认值就像"自动售货机的默认选项"——你什么都不按，它就给你默认的商品；你按了别的按钮，就给其他的。
>
> 默认值特别适合：
> - 大部分情况下参数值都一样，但偶尔需要改（如：`timeout: Int64 = 30`）
> - 函数有很多参数，但大多数参数有合理的默认值

---

## 4.2.5 流操作符（让代码像流水线一样清晰）

仓颉提供了两个流操作符，用于简化数据的链式处理和函数组合。这是仓颉的一个特色功能。

### Pipeline 表达式 `|>`（数据流水线）

> [!TIP]
> `|>` 的英文名叫 pipeline，中文叫"流水线"——就像工厂里的传送带，原材料从一头进去，经过一道道工序，从另一头出来成品。

当需要对输入数据做一系列处理时，可以用 pipeline 表达式来让代码更直观。

**语法形式**：

```Cangjie
e1 |> e2
```

这等价于：

```Cangjie  
let v = e1
e2(v)
```

也就是：**把 `e1` 的结果作为参数，传给 `e2` 函数**。

**举个实际例子**——先给数组每个元素加 1，再求和：

```Cangjie
// 数组每个元素加 1
func inc(x: Array<Int64>): Array<Int64> {
    let s = x.size
    var i = 0
    for (e in x where i < s) {
        x[i] = e + 1
        i++
    }
    x
}

// 数组元素求和
func sum(y: Array<Int64>): Int64 {
    var s = 0
    for (j in y) {
        s += j
    }
    s
}

main() {
    let arr: Array<Int64> = [1, 3, 5]
    
    // 用 pipeline 的方式（推荐）：
    let res = arr |> inc |> sum      // 先 inc 再 sum —— 从左到右读，像流水线
    println(res)                     // 输出：12
    
    // 不用 pipeline 的方式（嵌套调用，从里往外读，容易晕）：
    // let res = sum(inc(arr))       // 先执行 inc(arr)，再把结果传给 sum
}
```

执行结果为：

```Bash
12
```

> [!NOTE]
> `arr |> inc |> sum` 看起来就像"数据在流动"：数组先流进 `inc` 处理，结果再流进 `sum` 求和。而嵌套写法 `sum(inc(arr))` 需要从最内层开始往外面读，不够直观。

### Composition 表达式 `~>`（函数组合）

composition 表达式表示**把两个单参函数组合成一个新函数**。

**语法形式**：

```Cangjie
f ~> g
```

这等价于：

```Cangjie
{ x => g(f(x)) }
```

也就是：先对 `x` 执行 `f`，再把结果传给 `g`。

> [!TIP]
> 可以把 `~>` 想象成"乐高积木"——把两个小积木（函数）拼在一起，变成一块大的积木（新函数）。

**示例**：

```Cangjie
func f(x: Int64): Float64 {
    Float64(x)          // Int64 → Float64
}

func g(x: Float64): Float64 {
    x + 1.0             // 加 1
}

var fg = f ~> g        // 组合：先把 Int64 转 Float64，再加 1
// fg 等价于 { x: Int64 => g(f(x)) }
```

> [!NOTE]
> 表达式 `f ~> g` 中，会先对 `f` 求值，再对 `g` 求值，最后才组合。组合后的新函数类型是 `(Int64) -> Float64`。

### 流操作符与命名参数的配合

流操作符**不能**与无默认值的命名形参函数直接一起使用——因为无默认值的命名形参函数必须给出命名实参才能调用：

```Cangjie
func f(a!: Int64): Unit {}

var a = 1 |> f // Error
```

通过 lambda 表达式可以解决（lambda 会在后续文章介绍）：

```Cangjie
func f(a!: Int64): Unit {}

var x = 1 |> { x: Int64 => f(a: x) } // ✅ 用 lambda 传命名参数
```

同样，当命名参数有默认值时，直接与流操作符一起使用也是错误的：

```Cangjie
func f(a!: Int64 = 2): Unit {}

var a = 1 |> f // Error
```

但是如果命名形参都有默认值，且函数只需要传入非命名形参，则可以与流操作符一起使用：

```Cangjie
func f(a: Int64, b!: Int64 = 2): Unit {}

var a = 1 |> f // ✅ OK，a 是非命名参数
```

> [!TIP]
> **简单记忆**：`|>` 只能传递一个值。如果函数除了这个值还需要其他命名参数，就得用 lambda 包装一下。

---

## 4.2.6 变长参数（一次传任意多个值）

变长参数是一种特殊的函数调用语法糖。当形参的**最后一个非命名参数**是 `Array` 类型时，实参中对应位置可以直接传入参数序列来代替手动写 `[]`：

```Cangjie
func sum(arr: Array<Int64>) {
    var total = 0
    for (x in arr) {
        total += x
    }
    return total
}

main() {
    println(sum())          // 0 个参数 → arr = []
    println(sum(1, 2, 3))   // 3 个参数 → arr = [1, 2, 3]
}
```

程序输出：

```Bash
0
6
```

> [!TIP]
> 变长参数就像"超市的自助称重"——你可以放一个苹果（1 个参数），也可以放一堆苹果（多个参数），称重台都能处理。
>
> 如果没有变长参数，你就得自己写 `sum([])` 和 `sum([1, 2, 3])`——多写一对方括号。变长参数帮你省掉了这步。

> [!IMPORTANT]
> - 只有**最后一个非命名参数**可以作为变长参数
> - 命名参数**不能**使用变长参数语法

```Cangjie
func length(arr!: Array<Int64>) {
    return arr.size
}

main() {
    println(length())        // Error: 命名参数不能变长
    println(length(1, 2, 3)) // Error: 命名参数不能变长
}
```

### 变长参数的使用范围

变长参数可以用在很多地方：

- ✅ 全局函数
- ✅ 静态成员函数
- ✅ 实例成员函数
- ✅ 局部函数
- ✅ 构造函数
- ✅ 函数变量和 lambda
- ❌ composition（`~>`）和 pipeline（`|>`）不支持

> [!NOTE]
> class 相关内容将在后续讲解，此处先留个印象。

```Cangjie
class Counter {
    var total = 0
    init(data: Array<Int64>) {
        total = data.size
    }
    operator func ()(data: Array<Int64>) {
        total += data.size
    }
}

main() {
    let counter = Counter(1, 2)    // 两个参数 → data = [1, 2]，total = 2
    println(counter.total)         // 输出：2
    counter(3, 4, 5)               // 三个参数 → data = [3, 4, 5]，total = 2 + 3 = 5
    println(counter.total)         // 输出：5
}
```

### 变长参数与函数重载

当同时有"普通版本"和"变长版本"的函数时，编译器会**优先匹配普通版本**：

```Cangjie
func f<T>(x: T) where T <: ToString {
    println("item: ${x}")
}

func f(arr: Array<Int64>) {
    println("array: ${arr}")
}

main() {
    f()       // 无参 → 只能匹配变长版本 → array: []
    f(1)      // 1 个参数 → 优先匹配普通版本 f<T>(x: T) → item: 1
    f(1, 2)   // 2 个参数 → 普通版本不匹配 → 变长版本 → array: [1, 2]
}
```

```Bash
array: []
item: 1
array: [1, 2]
```

> [!WARNING]
> 如果两个变长版本都能匹配，编译器无法决定用哪个，会报错。解决办法是给函数改名或者调整参数设计：
> ```Cangjie
> func f(arr: Array<Int64>) { arr.size }
> func f(first: Int64, arr: Array<Int64>) { first + arr.size }
> main() {
>     println(f(1, 2, 3)) // Error：两个变长函数都能匹配
> }
> ```

---

## 4.2.7 总结

| 知识点 | 说明 |
|--------|------|
| 基本调用 | `f(arg1, arg2, ...)`，按位置传参 |
| 非命名参数 | 按位置传参，顺序必须与定义一致——像"排队打饭" |
| 命名参数 | 按名称传参，使用 `p: e` 语法，顺序可调换——像"快递标签" |
| 默认参数 | 调用时可不传，使用定义时的默认值——像"自动售货机默认选项" |
| Pipeline `\|>` | `e1 \|> e2` 等价于 `e2(e1)`，数据像流水线一样从左流到右 |
| Composition `~>` | `f ~> g` 等价于 `{ x => g(f(x)) }`，像拼乐高一样组合函数 |
| 变长参数 | 最后一个非命名 Array 参数可接收 0 或多个实参——像"自助称重" |
| 重载决议 | 优先匹配非变长版本，不行才尝试变长版本 |
d:["$","$L11",null,{"content":"$12","collection":"Cangjie/4_函数","rootColPath":"Cangjie","allArticles":["$4:props:children:props:children:2:props:articles:0","$4:props:children:props:children:2:props:articles:1","$4:props:children:props:children:2:props:articles:2","$4:props:children:props:children:2:props:articles:3","$4:props:children:props:children:2:props:articles:4","$4:props:children:props:children:2:props:articles:5","$4:props:children:props:children:2:props:articles:6","$4:props:children:props:children:2:props:articles:7",{"slug":"hello-seline-blog","title":"Hello Seline Blog","description":"欢迎来到 Seline Blog，一个开源的个人博客！","date":"2024-01-15","tags":["Seline","Next.js","博客"],"sort":1,"published":true,"mtime":1782986047851.8804},"$4:props:children:props:children:2:props:articles:8","$4:props:children:props:children:2:props:articles:9","$4:props:children:props:children:2:props:articles:10","$4:props:children:props:children:2:props:articles:11","$4:props:children:props:children:2:props:articles:12","$4:props:children:props:children:2:props:articles:13","$4:props:children:props:children:2:props:articles:14","$4:props:children:props:children:2:props:articles:15","$4:props:children:props:children:2:props:articles:16","$4:props:children:props:children:2:props:articles:17","$4:props:children:props:children:2:props:articles:18","$4:props:children:props:children:2:props:articles:19","$4:props:children:props:children:2:props:articles:20","$4:props:children:props:children:2:props:articles:21","$4:props:children:props:children:2:props:articles:22","$4:props:children:props:children:2:props:articles:23","$4:props:children:props:children:2:props:articles:24","$4:props:children:props:children:2:props:articles:25","$4:props:children:props:children:2:props:articles:26","$4:props:children:props:children:2:props:articles:27","$4:props:children:props:children:2:props:articles:28","$4:props:children:props:children:2:props:articles:29","$4:props:children:props:children:2:props:articles:30","$4:props:children:props:children:2:props:articles:31","$4:props:children:props:children:2:props:articles:32"],"currentSlug":"Cangjie/4_函数/2_调用函数"}]
10:null
